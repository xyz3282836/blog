import{_ as n}from"./plugin-vue_export-helper-x3n3nnut.js";import{r as p,o as i,c,d as e,f as a,e as r,a as s}from"./app-HxvT2NkL.js";const o="/assets/image-20220619231054174-cDEUUxMt.png",d="/assets/image-20220619231243015-LitPOTrE.png",l="/assets/image-20220621012555335-FipK7mgN.png",h="/assets/image-20220621012843230-1pRpKfgJ.png",u="/assets/image-20220620213836921-hDOj7fSR.png",g="/assets/image-20220620224512319-Ac-4UoC0.png",m="/assets/image-20220620232733164-jAyUZUju.png",f="/assets/image-20220620233410368-gzNLhCSz.png",b="/assets/image-20220620233639263-b4HpMkZd.png",x="/assets/拥塞控制-慢启动-J92jhjF6.jpg",_="/assets/拥塞控制-拥塞避免-MO7FkIwP.jpg",v="/assets/拥塞控制-拥塞发生-超时重传-PF9Ey97L.jpg",k="/assets/拥塞控制-快速恢复-WImf1dYQ.jpg",y={},N=s('<h2 id="协议栈" tabindex="-1"><a class="header-anchor" href="#协议栈" aria-hidden="true">#</a> 协议栈</h2><h3 id="理论模型" tabindex="-1"><a class="header-anchor" href="#理论模型" aria-hidden="true">#</a> 理论模型</h3><p>osi 七层模型</p><p>tcp/ip 四层模型</p><p>四层模型比较通用，每层都有自己的协议头部</p><h3 id="以太网帧格式-数据链路层" tabindex="-1"><a class="header-anchor" href="#以太网帧格式-数据链路层" aria-hidden="true">#</a> 以太网帧格式 （数据链路层）</h3><p>我们常用 ethernet II (罗马数字)，Ethernet 2 格式的以太网帧最大 1518bytes，最小 64bytes。</p><p>ethernet II 帧格式：目的地址 6，源地址 6，协议 2，数据 46-1500，校验和 4。MTU: 1500</p><p>最小 6+6+2+46+4=64bytes，最大 6+6+2+1500+4=1518bytes。</p><p>区分不同帧格式，从 2type 两个自己算出的值来确认</p><p>ethernet II type &gt; 1536</p><p>同样的两个字节在 802.3，802.2 代表 length， 都是 &lt;= 1500。</p><p>ethernet II 的 type 指定了上层协议，如下</p><table><thead><tr><th>0x0800</th><th>ip</th></tr></thead><tbody><tr><td>0x0806</td><td>arp</td></tr><tr><td>0x8137</td><td>ipx</td></tr><tr><td>0x86dd</td><td>ipv6</td></tr><tr><td>0x8035</td><td>rarp</td></tr><tr><td>0x8864</td><td>pppoe</td></tr></tbody></table><h3 id="jumbo-frame-巨型帧" tabindex="-1"><a class="header-anchor" href="#jumbo-frame-巨型帧" aria-hidden="true">#</a> jumbo frame（巨型帧）</h3><p>最多 9000 个字节</p><h3 id="ip-报文格式-网络层" tabindex="-1"><a class="header-anchor" href="#ip-报文格式-网络层" aria-hidden="true">#</a> ip 报文格式（网络层）</h3><figure><img src="'+o+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h3 id="tcp-报文格式-传输层" tabindex="-1"><a class="header-anchor" href="#tcp-报文格式-传输层" aria-hidden="true">#</a> tcp 报文格式（传输层）</h3><figure><img src="'+d+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><figure><img src="'+l+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><figure><img src="'+h+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h3 id="tcp-syns-queue-and-accept-queue" tabindex="-1"><a class="header-anchor" href="#tcp-syns-queue-and-accept-queue" aria-hidden="true">#</a> tcp syns queue and accept queue</h3><p>client(syn_send) ---&gt;syn (syn_rcvd)server 放入 syns queue</p><p>client(established) &lt;---sync+ack</p><p>---&gt;ack established 从 syns queue 取出放入 accept queue</p><p>accept 从 accept queue 取出</p><p><code>半连接队列</code>大小由内核参数 /proc/sys/net/ipv4/<code>tcp_max_syn_backlog</code> 决定</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token punctuation">[</span>root@23 ~<span class="token punctuation">]</span><span class="token comment"># cat /proc/sys/net/ipv4/tcp_max_syn_backlog</span>
<span class="token number">128</span>
</code></pre></div><p>Linux 实现了一种称为 SYNcookie 的机制，通过 net.ipv4.tcp_syncookies 控制，设置为 1 表示开启。<strong>简单说 SYNcookie 就是将连接信息编码在 ISN(initialsequencenumber)中返回给客户端，这时 server 不需要将半连接保存在队列中，而是利用客户端随后发来的 ACK 带回的 ISN 还原连接信息，以完成连接的建立，避免了半连接队列被攻击 SYN 包填满</strong>。</p><p><strong>当这个队列满了，不开启 syncookies 的时候，Server 会丢弃新来的 SYN 包，而 Client 端在多次重发 SYN 包得不到响应而返回（<code>connection time out</code>）错误。但是，当 Server 端开启了 syncookies=1，那么 SYN 半连接队列就没有逻辑上的最大值了，并且/proc/sys/net/ipv4/tcp_max_syn_backlog 设置的值也会被忽略。</strong></p><p><code>全连接队列</code>大小由 min(<code>backlog</code>, <code>somaxconn</code>)</p><p>backlog 其实就是 listen 函数的参数决定，int listen(int sockfd, int <code>backlog</code>)</p><p>somaxconn 则是内核参数 /proc/sys/net/core/<code>somaxconn</code></p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token punctuation">[</span>root@23 ~<span class="token punctuation">]</span><span class="token comment"># cat /proc/sys/net/core/somaxconn</span>
<span class="token number">128</span>
</code></pre></div><p>可以通过</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token punctuation">[</span>root@23 ~<span class="token punctuation">]</span><span class="token comment"># ss -lnt &#39;sport = :443&#39;</span>
State    Recv-Q    Send-Q    Local Address:Port    Peer Address:Port
LISTEN   <span class="token number">0</span>         <span class="token number">128</span>                   *:443                *:*
</code></pre></div><p>send-q 就是 accept queue</p><figure><img src="`+u+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>如果 accept queue 满了，syns queue 没有满，那么有两种处理方式</p><p>取决于 /proc/sys/net/ipv4/tcp_abort_overflow</p><div class="language-text" data-ext="text"><pre class="language-text"><code>[root@23 ~]# cat /proc/sys/net/ipv4/tcp_abort_on_overflow
0
</code></pre></div><p>次内核参数为 0：第三次握手假定不处理，造成丢失最后一次握手 ack，那么 client 会重新发起 ack</p><p>为 1: 直接发 rst 包给客户端来终止连接，此时客户端会收到 <strong>104 Connection reset by peer</strong> 的错误，一般不会配置这种策略</p><p>rst 包就表示终止连接</p><p>也可以先设置 1 的处理方式，只要客户端出现 104 错误，就证明是全连接队列满的原因导致问题</p><p>也可以通过统计的命令查看 accept queue 的情况：netstat -s |egrep &quot;listen|LISTEN&quot;</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token punctuation">[</span>root@23 ~<span class="token punctuation">]</span><span class="token comment"># netstat -s |egrep &quot;listen|LISTEN&quot;</span>
    <span class="token number">4</span> SYNs to LISTEN sockets dropped
</code></pre></div><figure><img src="`+g+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure>',49),S={href:"https://cloud.tencent.com/developer/article/1721800",target:"_blank",rel:"noopener noreferrer"},P=s(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code> -h, –help 帮助
 -V, –version 显示版本号
 -t, –tcp 显示 TCP 协议的 sockets
 -u, –udp 显示 UDP 协议的 sockets
 -x, –unix 显示 unix domain sockets，与 <span class="token parameter variable">-f</span> 选项相同
 -n, –numeric 不解析服务的名称，如 “22” 端口不会显示成 “ssh”
 -l, –listening 只显示处于监听状态的端口
 -p, –processes 显示监听端口的进程<span class="token punctuation">(</span>Ubuntu 上需要 <span class="token function">sudo</span><span class="token punctuation">)</span>
 -a, –all 对 TCP 协议来说，既包含监听的端口，也包含建立的连接
 -r, –resolve 把 IP 解释为域名，把端口号解释为协议名称
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>ss -ltn 查看 listen 状态的 tcp 服务端口，端口默认为服务名称，n 就是强制显示端口号</p><p>ss -ltr r 就是把地址解释成域名</p><p>ss -ltrp 显示监听端口的进程</p><figure><img src="`+m+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>ss -tan state xxx 可以监听状态，其实就是指定某些 tcp 状态，过滤作用</p><div class="language-text" data-ext="text"><pre class="language-text"><code>[root@23 ~]# ss -tan state listening &#39;( sport = :443 )&#39;
Recv-Q    Send-Q    Local Address:Port    Peer Address:Port
0         128                   *:443                *:*
</code></pre></div><p>ss -tna 将显示所有 tcp 的状态</p><figure><img src="`+f+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><figure><img src="'+b+`" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><p>time-wait 等待 2msl，其实 last-ack 被关的一方也会等待 2msl，也解释了为啥 time-wait 要等待 2msl</p><p>msl 可以配置的，/proc/sys/net/ipv4/tcp_fin_timeout</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code><span class="token punctuation">[</span>root@23 ~<span class="token punctuation">]</span><span class="token comment"># cat /proc/sys/net/ipv4/tcp_fin_timeout</span>
<span class="token number">60</span>
</code></pre></div><p>2msl=60s，所以 msl 是 30s</p><h3 id="rfc" tabindex="-1"><a class="header-anchor" href="#rfc" aria-hidden="true">#</a> RFC</h3><p>request for comments</p><h3 id="mss-mtu" tabindex="-1"><a class="header-anchor" href="#mss-mtu" aria-hidden="true">#</a> MSS，MTU</h3><p>如果底层物理接口 MTU= 1500 byte，则 MSS = 1500- 20(IP Header) -20 (TCP Header) = 1460 byte，如果 application 有 2000 byte 发送，需要两个 segment 才可以完成发送，第一个 TCP segment = 1460，第二个 TCP segment = 540。实际场景下，TCP 包头会带有 12 字节的时间戳，于是单个 TCP 包实际传输的最大量就会缩减为 1448 字节，1448=1500-20 字节（IP 头）-32 字节（20 字节 TCP 头和 12 字节 TCP 时间戳）</p><h2 id="tcp-协议滑动窗口" tabindex="-1"><a class="header-anchor" href="#tcp-协议滑动窗口" aria-hidden="true">#</a> tcp 协议滑动窗口</h2><h4 id="发送方" tabindex="-1"><a class="header-anchor" href="#发送方" aria-hidden="true">#</a> 发送方</h4><p>SND.WND</p><p>SND.UNA</p><p>SND.NXT</p><p>可用窗口= SND.WND-(SND.NXT-SND.UNA)</p><h4 id="接收方" tabindex="-1"><a class="header-anchor" href="#接收方" aria-hidden="true">#</a> 接收方</h4><p>rcv.wnd</p><p>rcv.nxt</p><p>操作系统缓冲区和滑动窗口</p><p>接收方，缓冲区有 t1 数据没读那么滑动窗口就有减去 t1</p><p>糊涂窗口综合症</p><h4 id="接收方-1" tabindex="-1"><a class="header-anchor" href="#接收方-1" aria-hidden="true">#</a> 接收方</h4><p>窗口小于 min(mss,缓存空间 1/2)，就会发送窗口为 0，等处理一些数据后，使得窗口&gt;=mss，或者缓存空间有一般可用</p><h4 id="发送方-1" tabindex="-1"><a class="header-anchor" href="#发送方-1" aria-hidden="true">#</a> 发送方</h4><p>Negle 算法</p><p>原则就是延迟处理</p><p>(窗口&gt;=mss || 数据大小&gt;=mss) || ack</p><h3 id="拥塞控制" tabindex="-1"><a class="header-anchor" href="#拥塞控制" aria-hidden="true">#</a> 拥塞控制</h3><p>网络环境差的时候，发送发不能无休止的重发数据包，需有有一定控制，所以有了<code>拥塞控制</code></p><h4 id="拥塞窗口" tabindex="-1"><a class="header-anchor" href="#拥塞窗口" aria-hidden="true">#</a> 拥塞窗口</h4><p>为了调节发送方发送数据，就有了拥塞窗口，也就是具体实现拥塞控制的实现，所以发送窗口是综合后的一个窗口</p><p>发送方维护一个拥塞窗口 swnd=min(cwnd,rwnd)，<code>发送窗口</code>为拥塞窗口和接受窗口的最小值</p><p>如何知道当前是否拥塞：没有在规定时间接受方没有 ack</p><p>拥塞控制主要有四个算法，本质就是控制拥塞窗口的值</p><h5 id="慢启动" tabindex="-1"><a class="header-anchor" href="#慢启动" aria-hidden="true">#</a> 慢启动</h5><p>原则：发送方没收到一个 ack，拥塞窗口就+1</p><p>每次可以发送原有基础上指数增长，有个上限 ssthresh （slow start threshold），一般 65535 字节</p><p>小于 ssthresh 就是慢启动，大于等于就会使用下一个拥塞避免算法</p><figure><img src="`+x+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h5 id="拥塞避免" tabindex="-1"><a class="header-anchor" href="#拥塞避免" aria-hidden="true">#</a> 拥塞避免</h5><p>原则：没收到一个 ack，cwnd 增加 1/cwnd，本质就是从慢启动的指数增长变成线性增长，虽然还是增长，但是速度变缓了</p><p>当 8 个 ack，增加了 1/8*8=1，所以 swnd 是 9，从而变成了线性增长</p><p>如果一直这么增长上去，会慢慢进入拥塞状态，出现丢包，会发生重传？为啥</p><p>当触发了重传，就进入下一个拥塞发生算法了</p><figure><img src="'+_+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h5 id="拥塞发生" tabindex="-1"><a class="header-anchor" href="#拥塞发生" aria-hidden="true">#</a> 拥塞发生</h5><p>重传有两种：超时重传和快速重传</p><h6 id="超时重传" tabindex="-1"><a class="header-anchor" href="#超时重传" aria-hidden="true">#</a> 超时重传</h6><p>当出现丢包时，发送方接受不到 ack 是，ssthresh 设置为 cwnd/2</p><p>cwnd 重设为 1</p><p>并且重新进入慢启动了，这种方式太激进</p><figure><img src="'+v+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h6 id="快速重传" tabindex="-1"><a class="header-anchor" href="#快速重传" aria-hidden="true">#</a> 快速重传</h6><p>比超时重传更好的方式，当接受方发现丢了一个中间包，就发送三次前一个包的 ack 给发送方，发送方就会重新快速重传</p><p>此时这种情况不严重，只是小部分数据包收到影响，所以</p><p>cwnd=cwnd/2</p><p>ssthresh=cwnd</p><p>并且进入下一个快速恢复</p><h5 id="快速恢复" tabindex="-1"><a class="header-anchor" href="#快速恢复" aria-hidden="true">#</a> 快速恢复</h5><p>一般和拥塞发生的快速重传一起使用</p><p>cwnd=ssthresh+3</p><p>重新传丢失的包</p><p>如果再受到重复的 ack，cwnd+1</p><p>如果收到新数据 ack，那么说明恢复正常了，cwnd 就恢复到拥塞避免的水平</p><figure><img src="'+k+'" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure>',74);function q(w,I){const t=p("ExternalLinkIcon");return i(),c("div",null,[N,e("p",null,[a("ss 命令"),e("a",S,[a("https://cloud.tencent.com/developer/article/1721800"),r(t)])]),P])}const z=n(y,[["render",q],["__file","tcpip.html.vue"]]);export{z as default};
